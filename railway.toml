[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py backfill_loaders && python manage.py collectstatic --noinput && gunicorn lcelist.wsgi --bind 0.0.0.0:$PORT"